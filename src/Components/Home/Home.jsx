import PropTypes from 'prop-types';
import Nav from '../Nav/Nav';
import Cards from '../Cards/Cards';

const Home = () => {
    return (
        <div>
            <Nav></Nav>
            <Cards></Cards>
        </div>
    );
};

Home.propTypes = {

};

export default Home;